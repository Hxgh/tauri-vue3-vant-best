# 🚀 快速开始指南

## 📱 连接小米手机后的快速操作

### 第一步：检查环境 ✅

```bash
pnpm android:check
```

或者

```bash
./android-check.sh
```

### 第二步：连接手机 📱

1. **开启 USB 调试**（仅首次需要）
   - 设置 → 我的设备 → 全部参数
   - 连续点击「MIUI 版本」7次
   - 设置 → 更多设置 → 开发者选项
   - 开启「USB 调试」

2. **连接手机**
   - 用数据线连接手机到 Mac
   - 手机上授权 USB 调试
   - ✅ 勾选「一律允许使用这台计算机进行调试」

3. **验证连接**
   ```bash
   adb devices
   ```
   
   应该看到：
   ```
   List of devices attached
   xxxxxxxx    device
   ```

### 第三步：启动调试 🔥

**选项 A：一键启动**（推荐）

```bash
pnpm android:dev
```

**选项 B：分开运行**（便于查看日志）

终端 1：
```bash
pnpm dev
```

终端 2：
```bash
pnpm android:dev
```

---

## 📦 构建 APK

### Debug 版本
```bash
pnpm android:build
```

### Release 版本
```bash
pnpm android:build:release
```

---

## 🎯 常用命令

| 命令 | 说明 |
|------|------|
| `pnpm dev` | 启动 Web 开发服务器 |
| `pnpm android:check` | 检查 Android 环境 |
| `pnpm android:dev` | Android 调试模式 |
| `pnpm android:build` | 构建 Debug APK |
| `pnpm android:build:release` | 构建 Release APK |
| `adb devices` | 查看连接的设备 |
| `adb logcat` | 查看手机日志 |

---

## ⚡ 预期效果

应用安装后，你会看到：
- ✅ 顶部导航栏（适配刘海屏）
- ✅ 可滚动的内容区域
- ✅ 底部标签栏（适配虚拟按键）
- ✅ 功能演示按钮（Toast、Dialog、Notify）

---

## 🐛 遇到问题？

### 问题 1：手机连接不上
```bash
adb kill-server
adb devices
```

### 问题 2：编译失败
```bash
# 清理并重试
cd src-tauri/gen/android
./gradlew clean
cd ../../..
pnpm android:dev
```

### 问题 3：应用安装失败
```bash
# 卸载旧版本
adb uninstall com.express.app
# 重新调试
pnpm android:dev
```

**详细排查请查看**：`ANDROID_DEBUG.md`

---

## 📚 文档索引

- **`README.md`** - 项目介绍
- **`MOBILE_SETUP.md`** - Vant 和安全区域配置说明
- **`ANDROID_DEBUG.md`** - Android 调试完整指南（推荐详读）
- **`QUICK_START.md`** - 本文档，快速参考

---

**准备好了吗？连接手机，运行 `pnpm android:check`！🎉**


