# 布局系统

## 核心配置

```typescript
interface MainLayoutProps {
  headerMode: 'none' | 'standard' | 'immersive';
  contentStart: 'screen-top' | 'safe-area' | 'below-header';
  tabbarMode: 'none' | 'standard' | 'immersive';
}
```

---

## 五种布局模式

### 模式 1：标准页面（Header + Tabbar）

**配置：**
```typescript
HeaderMode: Standard
ContentStart: BelowHeader  
TabbarMode: Standard
```

**视觉效果：**
```
┌─────────────────────────────────┐
│ ████ 状态栏安全区域 ████         │ ← 自动处理
├─────────────────────────────────┤
│ ← 发现                    ⋯    │ ← Header 固定
├─────────────────────────────────┤
│  📝 内容可滚动                   │
│  📝 列表项 1                     │
│  📝 列表项 2                     │ ← Header 不动
│  ...                            │
├─────────────────────────────────┤
│ 🏠 首页  🔍 发现  ⚙️ 设置       │ ← Tabbar 固定
│ ████ 底部安全区域 ████           │ ← 自动处理
└─────────────────────────────────┘
```

**示例：** Page2

**自动处理：**
- ✅ `padding-top`: `calc(46px + var(--sat))`
- ✅ `padding-bottom`: `calc(50px + max(var(--sab), 20px))`

---

### 模式 2：无 Header 页面（有 Tabbar）

**配置：**
```typescript
HeaderMode: None
ContentStart: SafeArea
TabbarMode: Standard
```

**视觉效果：**
```
┌─────────────────────────────────┐
│ ████ 透明占位（不可滚动）████    │ ← 固定不动
├─────────────────────────────────┤
│ 🎨 Banner（紫色背景）            │
│    设置                          │ ← 内容可滚动
│  ⚙️ 主题设置                     │
│  ...                            │
├─────────────────────────────────┤
│ 🏠 首页  🔍 发现  ⚙️ 设置       │ ← Tabbar 固定
│ ████ 底部安全区域 ████           │
└─────────────────────────────────┘
```

**示例：** Page3

**自动处理：**
- ✅ 固定占位元素 `.safe-area-spacer`
- ✅ `padding-top`: `var(--sat)`
- ✅ `padding-bottom`: `calc(50px + max(var(--sab), 20px))`

---

### 模式 3：标准详情页（Header + 固定按钮）

**配置：**
```typescript
HeaderMode: Standard
ContentStart: BelowHeader
TabbarMode: None
```

**视觉效果：**
```
┌─────────────────────────────────┐
│ ████ 状态栏安全区域 ████         │
├─────────────────────────────────┤
│ ← 详情页                  ⋯    │ ← Header 固定
├─────────────────────────────────┤
│  📦 商品图片                     │
│  💰 ¥299.00                     │ ← 内容可滚动
│  📝 商品详情                     │
│  ...                            │
├─────────────────────────────────┤
│ 🛒 加入购物车 | 💳 立即购买      │ ← FixedBottom 组件
│ ████ 底部安全区域 ████           │    自动处理
└─────────────────────────────────┘
```

**示例：** DetailPage

**自动处理：**
- ✅ `padding-top`: `calc(46px + var(--sat))`
- ✅ `padding-bottom`: `max(var(--sab), 20px)`

**使用 FixedBottom：**
```vue
<FixedBottom>
  <van-button type="primary">提交</van-button>
</FixedBottom>
```

---

### 模式 4：完全沉浸式（内容覆盖整屏）

**配置：**
```typescript
HeaderMode: None
ContentStart: ScreenTop
TabbarMode: Immersive
```

**视觉效果（登录页）：**
```
┌─────────────────────────────────┐
│ ← 登录 (透明导航栏，白色文字)    │ ← ImmersiveNavbar
│ ～～～～～～～～～～～～～～～～～ │    浮在背景上
│ ～～～ 紫色渐变背景 ～～～        │
│ ～～～～～～～～～～～～～～～～～ │
│       👤 Logo                   │
│       登录页示例                 │
├─────────────────────────────────┤
│  📱 用户名 ___________          │ ← 白色卡片
│  🔒 密码   ___________          │
│  🔵 登录                        │
│  (手动处理底部安全区域)          │
└─────────────────────────────────┘
```

**视觉效果（视频页）：**
```
┌─────────────────────────────────┐
│ ← 视频      ⋯ (透明导航栏)      │ ← ImmersiveNavbar
│                                 │
│          ▶️ 视频                 │ ← 视频覆盖整屏
│          画面                    │
│                                 │
│ ▶️  00:30 / 05:20   🔊  ⛶      │ ← 底部控制栏
│ (手动处理底部安全区域)           │    半透明浮层
└─────────────────────────────────┘
```

**示例：** LoginPage、VideoPage

**自动处理：** 无（完全沉浸式）

**手动处理：**
```vue
<!-- 导航栏：使用 ImmersiveNavbar 组件 -->
<ImmersiveNavbar>
  <template #left>
    <van-icon name="arrow-left" color="white" />
  </template>
  <template #title>
    <span style="color: white;">标题</span>
  </template>
</ImmersiveNavbar>

<!-- 底部内容：手动添加安全区域 -->
<div style="padding-bottom: calc(24px + env(safe-area-inset-bottom))">
  ...
</div>
```

---

## MainLayout 自动 Padding 规则

### 顶部

| HeaderMode | ContentStart | 自动处理 |
|-----------|--------------|----------|
| `None` | `SafeArea` | 固定占位 + `var(--sat)` |
| `None` | `ScreenTop` | 无 |
| `Standard` | `BelowHeader` | `calc(46px + var(--sat))` |

### 底部

| TabbarMode | 自动处理 |
|-----------|----------|
| `Standard` | `calc(50px + max(var(--sab), 20px))` |
| `None` | `max(var(--sab), 20px)` |
| `Immersive` | 无 |

**原则：** `Immersive` = 完全沉浸式，不处理安全区域；其他情况内容在安全区域内。

---

## 工具组件

### FixedBottom

固定底部按钮容器，自动处理安全区域。

```vue
<FixedBottom>
  <van-button>提交</van-button>
</FixedBottom>
```

### ImmersiveNavbar

沉浸式透明导航栏（顶部），适用于完全沉浸式页面。

```vue
<ImmersiveNavbar>
  <template #left>
    <van-icon name="arrow-left" color="white" />
  </template>
  <template #title>
    <span style="color: white;">标题</span>
  </template>
</ImmersiveNavbar>
```

### ImmersiveBottomBar

沉浸式透明底部栏（底部），适用于完全沉浸式页面。

```vue
<ImmersiveBottomBar>
  <div class="controls">
    <van-icon name="play" color="white" />
    <span style="color: white;">00:30 / 05:20</span>
  </div>
</ImmersiveBottomBar>
```

---

## 快速选择

| 需求 | 模式 |
|------|------|
| 标准列表页 | 模式 1 |
| 无导航首页 | 模式 2 |
| 详情页 + 固定按钮 | 模式 3 |
| 登录/视频/全屏 | 模式 4 |
